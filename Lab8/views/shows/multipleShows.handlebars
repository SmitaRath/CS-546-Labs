
  <h1>{{title}}</h1>
  <h2>{{searchTerm}}</h2>
  <main>
    <ul class="multiple-list">
     {{#each allShows}}
        <li><a href="/shows/{{show.id}}">{{show.name}}</a></li>
      {{/each}}
    </ul>
    <a href="/">Make another search</a>
</main>